<?xml version="1.0" encoding="UTF-8"?>
<!--
	Why this file exists: the build definition: dependencies, the Java version and build plugins.

	JS/TS vs Java: pom.xml is package.json + tsconfig + the build script in one XML file (Maven's
	"Project Object Model"). Three things to know:
	  - The <parent> (spring-boot-starter-parent) is a BOM (bill of materials): it pins mutually
	    compatible versions of hundreds of libraries, which is why dependencies below have no version
	    numbers. It is what stops "dependency hell" without a lockfile.
	  - "Starters" (spring-boot-starter-webmvc, ...) are one-line bundles of everything needed for a
	    feature: the web server, JSON, validation.
	  - `mvn package` compiles, runs the tests and builds ONE runnable "fat jar" containing the app
	    AND all its dependencies (the spring-boot-maven-plugin below), started with `java -jar`.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.resume</groupId>
	<artifactId>links</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>links</name>
	<description>Short link backend implementing the shared contract</description>
	<properties>
		<java.version>25</java.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<!-- Formatting: `mvn spotless:check` fails on unformatted code (run in CI) and
			     `mvn spotless:apply` fixes it. The Java counterpart of prettier/gofmt/ruff. -->
			<plugin>
				<groupId>com.diffplug.spotless</groupId>
				<artifactId>spotless-maven-plugin</artifactId>
				<version>2.46.1</version>
				<configuration>
					<java>
						<googleJavaFormat>
							<version>1.28.0</version>
						</googleJavaFormat>
					</java>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
